def inf():
    return '''Usage:
    python main.py <Option>
    
Option:
    scanport        Checks and generates a list of active/inactive/filtered ports
    pingsweep       Checks and generates a list of connected devices in current network
    extport         Checks and generates a list of active/inactive/filtered ports on another machine
    --help      -h  Loads this message'''
